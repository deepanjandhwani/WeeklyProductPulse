{
  "name": "weekly-product-pulse",
  "private": true,
  "description": "Monorepo: Python pipeline + Next.js in frontend/. Root lists next so Vercel detects Next.js when deploying from repo root.",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build -w frontend",
    "build:vercel": "npm run build -w frontend && node scripts/vercel-copy-next-to-root.cjs",
    "dev": "npm run dev -w frontend"
  },
  "dependencies": {
    "next": "^16.2.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
