# Weekly Product Pulse — dashboard

Next.js (App Router) UI for the WeeklyProductPulse API. See the repo root **`README.md`** for Vercel setup (Root Directory = `frontend`, env `PULSE_API_UPSTREAM`).

```bash
cp .env.example .env.local   # point at local or hosted API
npm install
npm run dev
```
